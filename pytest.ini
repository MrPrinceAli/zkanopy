[pytest]
testpaths = oracle/tests
addopts = -q
# The oracle steps import their shared path helper as `regions`; scripts get their own directory on
# sys.path automatically, pytest does not.
pythonpath = oracle
